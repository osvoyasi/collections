# Сравнение производительности: ArrayList vs LinkedList

## Обзор проекта

Лабораторная работа по сравнению производительности двух основных реализаций интерфейса List в Java: ArrayList и LinkedList. Проект предоставляет детальный анализ характеристик производительности коллекций для принятия обоснованных решений при выборе структур данных.

---

## Возможности

### Всестороннее тестирование
- **8 операций** протестировано для обоих типов коллекций
- **Несколько категорий операций**: Добавление, Получение, Удаление
- **Настраиваемые размеры тестов** 1000, 2000, 5000, 10000 операций
- **Тайминг с наносекундной точностью** для точных измерений

### Визуализация результатов
- **Форматированные таблицы** в консоли для легкой интерпретации
- **Сравнение производительности** с коэффициентами выигрыша/проигрыша
- **Анализ по категориям** с четкими паттернами
- **Определение победителей** для каждого типа операций

### Обеспечение качества
- **Покрытие тестами JUnit 5** для надежности
- **Проверки корректности** результатов
- **Прогрев JVM** для стабильных измерений

---

## Архитектура

collections/
├── src/
│   ├── main/
│   │   └── java/
│   │       └── collections/
│   │           ├── ListPerfTest.java    # Основной движок тестирования
│   │           └── Main.java            # Визуализация результатов
│   └── test/
│       └── java/
│           └── collections/
│               └── ListPerfTestTest.java # Набор тестов валидации
├── pom.xml                              # Maven конфигурация
└── README.md

---

## Тестируемые операции

### Операции добавления
| Операция | Описание | Сложность |
|----------|----------|-----------|
| `add(end)` | Добавление в конец | ArrayList: O(1)⁽*⁾, LinkedList: O(1) |
| `add(begin)` | Вставка в начало | ArrayList: O(n), LinkedList: O(1) |
| `add(middle)` | Вставка в середину | ArrayList: O(n), LinkedList: O(n) |

### Операции получения
| Операция | Описание | Сложность |
|----------|----------|-----------|
| `get(random)` | Случайный доступ | ArrayList: O(1), LinkedList: O(n) |
| `get(sequential)` | Последовательный доступ | ArrayList: O(1), LinkedList: O(n) |

### Операции удаления
| Операция | Описание | Сложность |
|----------|----------|-----------|
| `remove(end)` | Удаление с конца | ArrayList: O(1), LinkedList: O(n) |
| `remove(begin)` | Удаление с начала | ArrayList: O(n), LinkedList: O(1) |
| `remove(middle)` | Удаление из середины | ArrayList: O(n), LinkedList: O(n) |

### Прочие операции
| Операция | Описание | Сложность |
|----------|----------|-----------|
| `iteration` | Итерация по элементам | O(n) для обоих |

---

## Быстрый старт

### Предварительные требования
- **Java 11** или выше
- **Maven 3.6** или выше
- **JUnit 5** (автоматически через Maven)

### Сборка и запуск

 **Клонирование и навигация**
   git clone <URL-репозитория>
   cd collections

ArrayList vs LinkedList Performance Test         

Starting performance tests...
Warmup iterations: 1000
Test iterations: 10000

   PERFORMANCE RESULTS (in microseconds)
==========================================
Operation            | Iterations | ArrayList   | LinkedList  
---------------------|------------|-------------|-------------
add(end)             | 10000      | 1250        | 980        
add(begin)           | 10000      | 89200       | 1250       
add(middle)          | 1000       | 24500       | 15600      
get(random)          | 10000      | 450         | 12500      
get(sequential)      | 10000      | 320         | 480        
remove(end)          | 10000      | 650         | 1200       
remove(begin)        | 10000      | 89200       | 1250       
remove(middle)       | 1000       | 12300       | 8900       
iteration            | 10000      | 320         | 480        

 PERFORMANCE SUMMARY
======================

📈 WINNER BY OPERATION:
• add(end)          : ArrayList
• add(begin)        : LinkedList
• add(middle)       : LinkedList
• get(random)       : ArrayList
• get(sequential)   : ArrayList
• remove(end)       : ArrayList
• remove(begin)     : LinkedList
• remove(middle)    : LinkedList
• iteration         : ArrayList

FINAL SCORE:
ArrayList: 5 wins
LinkedList: 4 wins
  	
### Ключевые выводы

**ArrayList** обычно выигрывает в:

    Доступ по индексу (get) - O(1) против O(n)

    Итерация по элементам - лучшая локальность кэша

    Добавление/удаление в конце - амортизированная O(1)

    Потребление памяти - более компактное хранение

**LinkedList** обычно выигрывает в:

	Добавление/удаление в начале - O(1) против O(n)

    Добавление/удаление в середине - O(1) против O(n) при использовании итератора



